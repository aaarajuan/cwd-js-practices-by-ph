var numbers = [78, 45, 98, 45];
// 1. adding element in array: use .push() to add element in the last
// numbers.push(63);
console.log(numbers);

// use pop to get last element
numbers.pop(); // remove or ber kore fela element ke .pop() diye
console.log(numbers);